{{/*
Repository Analyzer - HTTP service backing Tool/repository-analyzer.
Service name is fixed so tools/repository-analyzer.yaml can use a stable FQDN.
*/}}
{{- if .Values.analyzer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.analyzer.name }}
  labels:
    {{- include "{{ .Values.project.name }}.labels" . | nindent 4 }}
    component: analyzer
spec:
  replicas: {{ .Values.analyzer.replicas }}
  selector:
    matchLabels:
      {{- include "{{ .Values.project.name }}.selectorLabels" . | nindent 6 }}
      component: analyzer
  template:
    metadata:
      labels:
        {{- include "{{ .Values.project.name }}.selectorLabels" . | nindent 8 }}
        component: analyzer
    spec:
      serviceAccountName: {{ include "{{ .Values.project.name }}.serviceAccountName" . }}
      {{- with .Values.security.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: analyzer
          image: "{{ .Values.analyzer.image.repository }}:{{ .Values.analyzer.image.tag }}"
          imagePullPolicy: {{ .Values.analyzer.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.analyzer.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.analyzer.port | quote }}
            - name: LOG_LEVEL
              value: {{ .Values.logging.level | quote }}
          {{- with .Values.security.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: workspace
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            {{- toYaml .Values.analyzer.resources | nindent 12 }}
      volumes:
        - name: workspace
          emptyDir:
            sizeLimit: {{ .Values.analyzer.workspaceSizeLimit }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.analyzer.name }}
  labels:
    {{- include "{{ .Values.project.name }}.labels" . | nindent 4 }}
    component: analyzer
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.analyzer.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "{{ .Values.project.name }}.selectorLabels" . | nindent 4 }}
    component: analyzer
{{- end }}
